type User {
	userId: ID
	username: String
	email: String
	registerDate: String
}

input UserInput {
	username: String!
	email: String!
	password: String!
}
extend type Query {
	userById(id: ID!):User
	userByEmail(email: String!):User
}

extend type Mutation {
	createUser(input: UserInput): User
}
